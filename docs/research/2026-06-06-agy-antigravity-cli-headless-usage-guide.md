# `agy` (Antigravity CLI) — Headless Usage + Backend Evaluation

> Research date: **2026-06-06** · Binary probed: **agy v1.0.6** (`~/.local/bin/agy`, 138 MB, rebuilt today) · Repo: `google-antigravity/antigravity-cli` (closed-source binary; 890★, 266 open issues, pushed 2026-06-06)
> Purpose: ground the planned `bin/agy-agent` wrapper (see `[[project-x-gemini-agy-migration]]`) in **verified** facts, not LLM extrapolation.
>
> **This doc has two parts.** **Part 1 (§1–11)** is the binary-verified headless *usage* reference. **Part 2 (§12–16)** is a live 3-backend *evaluation* (agy vs gemini-agent vs omo-explore) that stress-tested those facts on a real research task, plus how to re-run it. The runnable harness lives at `docs/research/agy-eval-harness/` (`run.sh`, `score.sh`, `prompt.txt`; `out/` is generated by a run, not committed — the captured evidence is in §13). Part 2 supersedes Part 1 wherever a figure was re-measured (notably latency — see §3/§10 reconciliation notes).

## Source-reliability note (read first)

agy has **no public granular docs**. The official site (antigravity.google/docs) is a JS-SPA that can't be scraped; the GitHub repo is changelog + README + TUI examples only; DeepWiki has overview text only; a `perplexity_research` lane **timed out at 240s**; and a Google-grounded `gemini-agent` lane **hallucinated non-existent flags** (`--json`, `--grounding`, `--output-format`, exit codes `429`/`130`, `.antigravityignore`). Every one of those was **refuted directly against the binary**. → **Trust order: the installed binary + live behavior tests + the embedded `agy changelog` > everything else.** Anything below marked "⚠ unverified" was not confirmable against ground truth.

---

# Part 1 — Headless Usage Guide (binary-verified)

## 1. Headless invocation (VERIFIED via `agy --help` + live runs)

```bash
agy -p "<prompt>"                         # --print: one-shot, prints response to stdout, then exits
agy --model "<name>" -p "<prompt>"        # per-call model selection (added in 1.0.5)
agy --add-dir <path> -p "<prompt>"        # mount a directory into the workspace (repeatable)
agy -c -p "<follow-up>"                   # --continue most recent conversation
agy --conversation <id> -p "<follow-up>"  # resume a specific conversation by id
agy --print-timeout <dur> -p "<prompt>"   # wait cap for print mode (default 5m0s, e.g. 120s)
agy --sandbox -p "<prompt>"               # terminal restrictions (now propagates in -p as of 1.0.6)
agy --dangerously-skip-permissions -p ... # auto-approve ALL tool/edit confirmations (YOLO)
agy --log-file <path> -p "<prompt>"       # override CLI log path
```

- **The prompt MUST be the value of `-p`/`--prompt`.** A bare positional prompt is silently ignored and agy emits a canned greeting (e.g. "I am currently using Gemini 3.1 Pro…") — verified in Part 2. Always `agy -p "<prompt>"`, never `agy "<prompt>"`.
- **There is NO structured/JSON output.** Print mode emits **markdown/plain text only**. `--json` / `--output-format` do **not exist** (refuted: `flags provided but not defined: -json`). Any downstream parser must consume plain text.
- **Print mode buffers the whole response to the end** (no streaming). A hard `timeout` that fires mid-run loses *everything* (exit 124, 0 bytes). Budget `--print-timeout`/outer timeout generously (§12 saw this masquerade as a "hang").
- **Output chrome is prompt-controlled.** With a normal prompt agy may append a `### Work Summary` block; an explicit "Output ONLY the list, no preamble or summary" suppresses it (live-verified). A wrapper must either instruct-to-suppress or strip trailing chrome.
- **Permission hang is conditional, not universal.** Pure text/Q&A in `-p` returns fine **without** `--dangerously-skip-permissions` (live-verified). Read-only tools (Google Search, file reads via `--add-dir`) also auto-run. The hang risk is only when the agent wants **shell exec or file edits** in a non-TTY → it blocks forever on a hidden Y/N. For a read-only advisor wrapper, do **not** pass `--dangerously-skip-permissions`; for autonomous build/test tasks, you must.

## 2. Models (VERIFIED via `agy models`)

```
Gemini 3.5 Flash (Medium|High|Low)     # fast tier; "Medium" is the practical default
Gemini 3.1 Pro (Low|High)              # reasoning tier
Claude Sonnet 4.6 (Thinking)           # cross-provider — not available in bare gemini CLI
Claude Opus 4.6 (Thinking)
GPT-OSS 120B (Medium)
```

- The thinking level is **baked into the model string** (e.g. `"Gemini 3.5 Flash (Low)"`), not a separate flag. This *is* the headless determinism/latency knob — pick the level by choosing the string.
- Pass the **exact display string** to `--model` (quote it). Default model for headless = the `model` key in settings.json (§4).
- Cross-provider menu (Claude + GPT-OSS) is a genuine advantage over the bare `gemini` CLI.

## 3. Google Search grounding + browser (VERIFIED live + binary protobuf)

- Grounding is **prompt/agent-driven, NOT a flag** (`--grounding` refuted). Including "Use Google Search" / "Use web search" in the prompt triggers it; the model also self-elects when the question needs fresh facts. Live test returned a correctly cited answer (Bun v1.3.14, source URLs).
- **Grounding is load-bearing for currency, not optional.** Part 2 (§14, finding 1) found that *without* an explicit web-search nudge, agy trusts the local repo over live docs and returned a **stale** identifier the repo still contained. For any "what's current / latest version / GA API" question, inject "Use Google Search to verify against current docs."
- The binary embeds a first-class `GoogleSearch` tool (`google_search` protobuf field) **and a full CDP/Chrome browser** (`cdp.Browser`, `BROWSER_OPEN`, `web_search_url`, `browser_page/text`) — so agy can *browse and scrape pages*, not just hit the Search grounding API. Richer than gemini CLI's search-only grounding.
- **Latency (reconciled with Part 2):** a *grounded web* flash-medium query took **105s** vs the gemini wrapper's **29s** (one sample) — the source of the "3–5×" figure. But a *scoped repo-grounding* run measured **79s vs 37s ≈ 2×** (§13). So: ~2× for ordinary scoped repo research; 3–5× only for web-grounded/agentic-heavy runs (and far worse under the §14 pathologies). Budget `--print-timeout` accordingly.

## 4. settings.json (`~/.gemini/antigravity-cli/settings.json`) — keys VERIFIED from binary strings + changelog

```jsonc
{
  "model": "Gemini 3.5 Flash (Medium)",   // default model for headless runs — set this to avoid relying on --model
  "colorScheme": "dark",                   // set/strip ANSI when parsing stdout
  "statusLine": { "type": "", "command": "", "enabled": true, "stack_with_default": false },
  "trustedWorkspaces": ["/abs/path", ...], // dirs agy trusts without the trust prompt — REQUIRED for non-interactive use
  "mcpServers": { ... },                   // inline MCP block — key present in binary; relationship to config/mcp_config.json (§5) is ⚠ unverified (both exist; precedence untested)
  "hooks": { ... },                        // hooks key present in binary; ⚠ exact schema/semantics unverified
  "UseG1Credits": true                     // overflow to Google One (G1) credits when Ultra quota is exhausted (1.0.3)
}
```

- **`trustedWorkspaces` is the load-bearing one for automation** — agy prompts to "trust" an unknown workspace interactively; pre-seeding the path (or `--skip-trust` in the bare gemini equivalent) avoids a hang. This machine already trusts `~`, `~/Codes/x-skills`, `~/Codes/oneclaw`. (Note: trust being present is necessary but NOT sufficient — §14 shows a trusted-but-huge `--add-dir` still hangs, for a different reason.)
- Permissions are now a merged system (1.0.5 `/permissions`): CLI `settings.json` + user settings shared with Antigravity GUI + project-level. ⚠ exact `permissions` sub-schema unverified.

## 5. MCP servers — `~/.gemini/config/mcp_config.json` (DISK-VERIFIED)

- **Confirmed on disk:** the live CLI MCP config is `~/.gemini/config/mcp_config.json` (296 B, present). There is **no** `~/.gemini/antigravity-cli/config/` dir — the `config/mcp_config.json` the changelog refers to is the shared `~/.gemini/config/` tree, not an agy-local one. (The GUI variants keep their own copies at `~/.gemini/antigravity{,-ide,-backup}/mcp_config.json`; agy CLI runtime state is separate under `~/.gemini/antigravity-cli/mcp/`.)
- Path migrated to `config/mcp_config.json` (changelog 1.0.3: writing to the legacy bare path silently no-ops). Supports **stdio** servers (`command`/`args`) and, since 1.0.5, **`url`** remote/SSE servers. Parallel init since 1.0.4.

## 6. Plugins / skills / agents (changelog 1.0.1/1.0.2 + `agy plugin --help`)

```bash
agy plugin import gemini   # non-destructively port gemini-cli plugins/extensions/MCP/themes
agy plugin import claude   # same for claude
agy plugin install <name>@<marketplace>   # marketplace install
agy plugin list | enable <n> | disable <n> | uninstall <n> | validate [path]
```

- Installed plugins/skills live in the **shared** `~/.gemini/config/` (1.0.2 fix — *not* `~/.gemini/antigravity-cli/plugins/`, which the hallucinated guide claimed). **DISK-VERIFIED:** `~/.gemini/config/skills/` exists with ~924 entries (each a skill directory, e.g. `wordpress-plugin-development/`); this is the same shared tree gemini-cli uses.
- agy auto-discovers **skills and specialized subagents** from installed plugin dirs (1.0.1) and supports fallback skill discovery in standalone mode (1.0.2). ⚠ The authoring **format** ("Markdown SKILL.md blueprint") came from the unreliable web lane and is **unverified** — only the *directory location* above is confirmed. The `agy plugin import gemini` reply on this machine was "No gemini extensions found", i.e. it imports gemini-cli *extensions/skills*, not the gemini binary/flags.
- **Note:** since agy imports gemini-cli & claude plugins/skills, x-skills' own skills could in principle be surfaced to agy — worth a follow-up probe if we ever want agy to run x-skills natively.

## 7. Conversations / resume (changelog 1.0.4)

- Conversation store is **SQLite** (`.db` / `.db-wal`, changelog 1.0.4) under **`~/.gemini/antigravity-cli/conversations/`** (DISK-VERIFIED: dir present with ~23 entries). Print-mode metadata writes to `~/.gemini/antigravity-cli/cache` (1.0.5 fix — previously polluted CWD). Logs at `~/.gemini/antigravity-cli/log/` (symlinked as `cli.log`).
- Resume the latest with `-c`; resume a specific id with `--conversation <id>`. Subagent conversations are excluded from the `/resume` picker (1.0.6). ⚠ how to *capture* the conversation id from a headless `-p` run is unverified (bare gemini prints a session UUID; agy's headless id surfacing is unconfirmed — likely needs reading the SQLite store or log).

## 8. Auth & quota

- OAuth via system keyring → Google Sign-In fallback (README). No `GEMINI_API_KEY`-style env for the consumer path. Enterprise = connect a GCP project / Gemini Code Assist license during onboarding.
- This machine: `~/.gemini/antigravity-cli/cache/onboarding.json` shows `consumerOnboardingComplete` **and** `enterpriseOnboardingComplete` = true → headless runs work without re-auth. **⚠ Caveat:** because enterprise onboarding is complete here, the live latency/success numbers in this doc may draw on **enterprise (Gemini Code Assist) quota**, not the **Ultra-only** path the migration actually targets. An Ultra-only user could see different quota/rate-limit behavior — re-benchmark on an Ultra-only profile before trusting these figures for the consumer case.
- **G1 credit overflow** (`UseG1Credits: true`): when the weekly Ultra quota is exhausted, agy falls back to Google One AI credits instead of hard-failing — directly relevant to headless reliability. `/usage` `/quota` `/credits` are interactive-only.
- **The log lies about auth.** agy's log (`~/.gemini/antigravity-cli/log/`) emits `error getting token source: You are not logged into Antigravity` and `failed to set auth token` on **every** run — *including successful ones* (verified in §14). Those lines are for auxiliary `loadCodeAssistResponse` / `fetchAdminControls` / `ListExperiments` caches, **not** the model-serving path. **Do not classify failures on those strings** — they are noise.
- Strategic context: the bare `gemini` CLI stops serving Ultra/Pro/free on **2026-06-18**; agy is the successor (`[[project-x-gemini-agy-migration]]`). Enterprise onboarding here means agy is not dependent on the dying Ultra-serving path.

## 9. Error handling in headless mode (VERIFIED behavior + prior-session finding)

- Success exits **0** with the response on stdout (verified). The hallucinated `429`/`130` exit codes are **not** real (`--bogus` control proves undefined-flag handling; no evidence of HTTP-style exit codes).
- **agy returns exit 0 even on failures** (prior session: exit 0 on "invalid project ID" / "not authenticated"; §14: exit 0 / exit 124 with empty stdout on traversal hangs). → **Do not rely on exit codes for failure detection.** A robust wrapper must: (a) treat **empty/whitespace stdout** as failure, and (b) tail `--log-file` for failure markers — but **first strip the always-present auth noise** (§8) so it can't mislabel a generic empty failure as an auth error. Order the classification quota/planner → narrow auth → generic empty. This is the single biggest wrapper-hardening requirement.
- One transient empty-stream failure was observed on the *bare gemini* path (`-m gemini-2.5-flash`), not agy — but the empty-output failure mode exists on both; the wrapper's empty-stdout guard covers it.

## 10. Gotchas for headless / CI

1. **No JSON** → parse plain text; strip ANSI (`colorScheme`) and any trailing `### Work Summary` chrome.
2. **Exit 0 lies** → gate on empty-stdout + log-tail, never exit code (§9). Strip the §8 auth noise before classifying.
3. **Trust prompt** → pre-seed `trustedWorkspaces` or the run hangs on first use of an untrusted dir.
4. **Permission hang** → only when the agent wants shell/edit in non-TTY; pass `--dangerously-skip-permissions` for autonomous tasks, omit it for read-only advisory.
5. **Never `--add-dir` a large tree** → §14: a repo-root `--add-dir` (65 GB monorepo w/ node_modules) hung agy 15–25 min, 0 bytes; a scoped 572 KB subtree returned in 79 s. Scope to the relevant subdirs.
6. **Serialize agy calls** → §14: agy spawns a local gRPC language-server per invocation; concurrent calls contend and hang. Fan-out consumers (x-research Max Mode, x-qa) must run agy lanes sequentially.
7. **Latency** → ~2× the gemini wrapper for a scoped grounded run (≈79 s vs ≈37 s); 3–5× only for web-grounded/agentic-heavy runs (105–146s vs 29s). Budget `--print-timeout`; route bulk/no-tool work to a `(Low)` model.
8. **Prompt as flag value** → `agy -p "<prompt>"`; a positional prompt is silently ignored (§1).
9. **Fast-moving target** → 266 open issues, binary rebuilt the same day; pin behavior with live smoke tests, don't assume changelog == current.

## 11. Implications for `bin/agy-agent` (the payoff)

A drop-in-shaped wrapper is feasible but must rebuild what the JSON path gave gemini:

| gemini-agent feature | agy equivalent | wrapper work |
|---|---|---|
| `.response` extraction | plain stdout | trivial (use stdout) — but strip chrome |
| stats / session-id / thoughts-answer counts (JSON) | none | **drop**; lose thinking-starvation telemetry |
| thinking-starvation mitigation (`thinkingLevel` in settings) | model string `(Low/Medium/High)` | re-express as model-alias map |
| `--model flash/pro` | `--model "Gemini 3.5 Flash (Medium)"` / `"Gemini 3.1 Pro (High)"` | alias map |
| `--system <file>` | none (uses plugin/skill model) | **gap** — emulate by prepending to prompt |
| `--file` inline `@file` | `--add-dir <dir>` | switch to dir mount (+ large-dir guard, §10.5) |
| exit-code error classes | exit 0 always | **rebuild**: empty-stdout + log-tail parser (strip auth noise, §8) |
| `--resume` (gemini `-r latest`) | `-c` / `--conversation <id>` | map; solve id capture (§7) |
| Google Search grounding | prompt-driven (no flag) | inject "Use Google Search" when grounding wanted (§3) |

**Recommended wrapper shape:** dual-track behind capability flag `agy_cli` (keep `gemini-agent` default until 2026-06-18), model-alias map (`flash`→`Gemini 3.5 Flash (Medium)`, `flash-low`→`(Low)`, `pro`→`Gemini 3.1 Pro (High)`, plus optional `claude`/`gpt-oss`), empty-stdout+log-tail failure detection (auth-noise-stripped), chrome-stripping, `trustedWorkspaces` preflight, large-`--add-dir` guard, call serialization, and `--system` emulated via prompt prepend. See the full task plan in `docs/superpowers/plans/2026-06-06-agy-agent-dual-track-backend.md`.

> **Side-fix for the *existing* gemini wrapper (independent of agy):** `skills/x-gemini/SKILL.md`'s Models table is stale on **both** aliases. Observed live: `gemini-agent --model flash` AND `--model pro` both resolved to `model=gemini-3-flash-preview` (stderr), and an explicit `-m gemini-2.5-flash` *failed* (empty stream). So `flash → gemini-2.5-flash` and `pro → gemini-3.1-pro-preview` no longer match reality — Gemini 3 model IDs have shifted and `pro` may be silently downgrading to flash. Re-pin both aliases (and verify `pro` actually serves a Pro-tier model) before relying on the gemini path in its remaining ~12 days.

---

# Part 2 — Backend Evaluation (live bake-off)

> Harness: `docs/research/agy-eval-harness/` (`run.sh`, `score.sh`, `prompt.txt`; `out/` is generated, not committed). Re-run with `./run.sh` (single process). Run date 2026-06-06.

## 12. Setup — why this test, and what it measures

Replays one identical research prompt — a real voice-recording architecture question from oneclaw session `9ef4a534`, where gemini-agent originally **fabricated** two identifiers — through three research backends, scoring each against two primary-source **anchors** with known-correct answers:

- **ANCHOR-1 (web/docs grounding):** the OpenAI Realtime GA event is `response.output_audio_transcript.done`. The repo contains only the *legacy* `response.audio_transcript.done` (+ a `response.output_item.done` decoy) — so grepping the repo gives the **wrong** answer. This tests whether a backend will leave the repo to check current docs.
- **ANCHOR-2 (repo grounding):** the real signature is `dispatchCoffer(deps, ctx, args)` (`extensions/oneclaw-multiagent/src/tools/coffer.ts:123`), called `dispatchCoffer(deps, {agentId}, {op:"write",...})`. gemini originally invented `dispatchCoffer(driver, path, stream)`.

Lanes: `gemini` (`gemini-agent --model pro`, file-attach only) vs `agy` (`agy -p … --model "Gemini 3.5 Flash (High)" --add-dir <scoped> --dangerously-skip-permissions`, agentic) vs `explore` (`opencode run --agent explore`, agentic — actually **openai/gpt-5.4-mini**, *not* Gemini Flash; the x-skills CLAUDE.md fallback table is stale on this).

## 13. Scored verdict

| Lane | Model | Mode | Wall time | ANCHOR-1 (GA event) | ANCHOR-2 (`dispatchCoffer`) |
|---|---|---|---|---|---|
| **explore** | gpt-5.4-mini | agentic (repo tool loop) | **141 s** | ✅ CORRECT `output_audio_transcript.done` | ✅ CORRECT `(deps, ctx, args)` literal def |
| **agy** | Gemini 3.5 Flash | agentic (scoped `--add-dir`) | **79 s** | ⚠️ LEGACY `audio_transcript.done` | ✅ CORRECT `(deps, ctx, args)` literal def |
| **gemini** | Gemini 3.1 Pro | file-attach only | **37 s** | ⚠️ LEGACY `audio_transcript.done` | ✅ CORRECT `(deps,{agentId},{op:write})` call-shape |

> **Two wrong diagnoses preceded this table** (full account in §14, finding 4): agy's empty outputs were first read as a "timeout / non-viable," then as an "auth failure." Both wrong. agy works (79 s, scoped). The empties were caused by `--add-dir` on the 65 GB whole repo + concurrent agy processes; the `not logged into Antigravity` log lines are a red herring (§8).

**Raw evidence (captured 2026-06-06; the `out/` run dir was deleted after transcribing this).** Verbatim `[ANCHOR]` lines each lane emitted, plus exit/wall/bytes from each `.meta`:

| Lane | exit | s | bytes | `[ANCHOR-1]` emitted (verbatim) | `[ANCHOR-2]` emitted (verbatim) |
|---|---|---|---|---|---|
| gemini | 0 | 37 | 3668 | `` `response.audio_transcript.done` `` | `` `dispatchCoffer(deps, { agentId }, { op: "write", path, content })` `` |
| agy | 0 | 79 | 9520 | `` `response.audio_transcript.done` `` | `export async function dispatchCoffer(deps: CofferDeps, ctx: CofferCtx, args: CofferArgs): Promise<ToolResult>` |
| explore | 0 | 141 | 3910 | `response.output_audio_transcript.done` | `export async function dispatchCoffer(deps: CofferDeps, ctx: CofferCtx, args: CofferArgs): Promise<ToolResult>` |

agy's transcript also shows it grounding before answering — *"I will start by searching the codebase to find where `dispatchCoffer` is defined and used"* — confirming the repo tool loop (vs gemini's file-attach). To reproduce, re-run the harness (§16); `out/` regenerates.

## 14. Findings

**1. Only one lane got the web/docs anchor right — and it isn't a Gemini lane.** `explore` (gpt-5.4-mini) alone returned the GA event, by going to OpenAI docs / the upstream provider. Both Gemini lanes (agy + gemini) returned the **legacy** value that lives in the repo. agy even grepped, *confirmed* "it only matched the design doc," then reported it anyway. Lesson: for facts that may be stale in your own repo, the lane must be willing to leave the repo. agy's grounding is prompt-driven, so ANCHOR-1 is likely recoverable with an explicit "Use Google Search" nudge (§3) — worth a follow-up run.

**2. The repo anchor: everyone passed, via different paths.** agy and explore opened `coffer.ts` and quoted the literal definition; gemini gave the correct *call-shape* only because the replay prompt pre-loaded "writes go through dispatchCoffer (ACL)" — richer scaffolding than the original session that produced the `dispatchCoffer(driver,path,stream)` invention. gemini's reliability scales with how much ground truth you hand it; agy/explore derive it from the tool loop.

**3. explore surfaced facts the others did not.** Only explore reported (a) `dispatchCoffer` is **string-content only — it can't write raw `.webm` bytes** (a hard implementation blocker), and (b) an existing design memo that already rejects the two-file approach. agy critiqued CPU hazards and lifecycle well but missed the binary-write blocker; gemini (file-attach) couldn't reach either, structurally.

**4. agy DOES work — two wrong diagnoses, corrected by instrumentation.** agy returned 0 bytes across many runs. Diagnosis went (a) "timeout/non-viable" → wrong; (b) "auth failure" (log screams `not logged into Antigravity`) → wrong; (c) the truth: agy works — 79 s, exit 0, 9.5 KB, both anchors answered, three files opened — when run as **a single clean process with a scoped `--add-dir`**. Actual causes of the empties:
- **`--add-dir` on the 65 GB whole repo** blew up agentic traversal → multi-minute hangs.
- **Concurrent agy processes** contend (per-call gRPC language-server). The "scoped also failed" run overlapped a 25-min whole-repo run; alone, the same scoped config returns in 79 s.
- The `not logged into Antigravity` lines appear verbatim on the *successful* run too — auxiliary caches, not serving (§8). The oauth token was valid throughout.

## 15. Recommendation

- **For grounded code/research today, prefer the agentic lanes (explore or agy) over gemini file-attach.** A smaller model that reads the files matched/beat the stronger model that can't. Keep gemini for *architecture critique* where you pre-load ground truth and treat every concrete identifier as unverified until grepped.
- **agy is a viable gemini-CLI replacement for repo-grounded research** — it grounds in the repo (gemini-agent can't) and is only ~2× slower (79 s vs 37 s), not the 3–5× feared. It ties gemini on the anchors and beats it on grounding capability.
- **agy's one gap is web/docs currency** (ANCHOR-1 legacy). For "what's the current API" questions, add "Use Google Search to verify against current docs" and re-run — likely closes the gap.
- **As the sole Google-model backend (straight replacement, no cutover), `bin/agy-agent` must:** scope `--add-dir`, serialize agy calls, and gate on empty-stdout + auth-noise-stripped log-tail (exit code lies). None are blockers — they're wrapper requirements, all folded into §11 and the task plan.

## 16. Running the harness (reproduce / regression-test)

The harness lives at `docs/research/agy-eval-harness/` — `run.sh`, `score.sh`, `prompt.txt` (the `out/` results dir is **generated** by `run.sh`, not committed; the 2026-06-06 run's evidence is preserved verbatim in §13). The two anchors (§12) are the **ground truth this harness regression-tests future backends against** — re-run it after any agy/opencode/model upgrade and require both anchors green.

```bash
cd docs/research/agy-eval-harness
./run.sh                # run all three lanes (cd's into $ONECLAW_REPO), then score
./score.sh              # re-score existing out/*.txt without re-running
TIMEOUT=1500 ./run.sh   # agy buffers output to the end; raise the per-lane cap if it truncates (exit 124)
```

- **Env:** `ONECLAW_REPO` (default `/Users/randytran/Codes/oneclaw`), `TIMEOUT` (per-lane seconds).
- **Run as a single process; do NOT parallelize lanes** — concurrent agy calls contend (§10.6).
- **Outputs:** `out/<lane>.txt` (raw answer), `out/<lane>.err` (stderr), `out/<lane>.meta` (exit/seconds/bytes), `out/RUN.log` (full run + score table).
- **Scorer note:** the ANCHOR-2 matcher allows TS type annotations (`deps: CofferDeps, ...`), so the literal `coffer.ts:123` definition scores CORRECT.
- **Gotchas baked in from building it:** prompt must be the value of `-p` (§1); `--print` buffers + a short timeout truncates to 0 bytes (§10.7); the `explore` agent is `openai/gpt-5.4-mini` not Gemini Flash (CLAUDE.md fallback table stale, §12).

---

### Appendix — claims REFUTED against ground truth (do not re-trust)
- `--json` / `--output-format` flag — **does not exist** (no structured output).
- `--grounding` flag — **does not exist** (grounding is prompt-driven).
- exit codes `429` / `130` — **not real**; agy exits 0 even on failure.
- `.antigravityignore` file, `verbosity` setting — **unconfirmed**, no binary/changelog evidence.
- plugins in `~/.gemini/antigravity-cli/plugins/` — **wrong**; they live in `~/.gemini/config/`.
- "Minimal" thinking level for Flash — `agy models` lists only Low/Medium/High for Flash.
- `not logged into Antigravity` / `failed to set auth token` log lines as an **auth-failure signal** — **red herring**; they appear on successful runs too (§8, §14).
- agy "is too slow / hangs / can't be authed" as a *capability* verdict — **wrong**; those were harness errors (huge `--add-dir`, concurrency), corrected in §14.

---

## Validation results (2026-06-08)

Task 10 smoke of the migrated `agy-agent` backend against the three consumer signal classes, run via the on-PATH `agy-agent` wrapper, **strictly serialized** (no concurrent agy calls). Latency/output are the **real observed values**, not estimates.

| Signal class | Command | Latency | Usable? | Grounding cited URLs? | `### Work Summary` chrome? | Parseable bare JSON? |
|---|---|---|---|---|---|---|
| library-current-state (x-research / x-guide) | `agy-agent --model flash-low --grounded "…zod… latest version + cite URLs"` | 600 s → **timeout** (exit 124) | **No** — 0 bytes stdout | n/a (no output) | n/a | n/a |
| multi-file review (x-review / x-bugfix) | `agy-agent --model pro --add-dir skills/x-gemini "top 3 risks as FILE:LINE"` | not run — agy serving path confirmed down (see below) | — | — | — | — |
| pass/fail judge (x-qa eval) | `agy-agent --model flash "Reply with ONLY this JSON: {\"score\": 0.9}"` | 600 s → **timeout** (exit 124) | **No** — 0 bytes stdout | n/a | n/a | n/a (no output) |

**Environmental outage, NOT a wrapper bug (verified at the raw-CLI level).** Both wrapper calls returned 0 bytes and the wrapper correctly synthesized `status=timeout` / exit 124. A raw-agy control probe (`agy -p "Reply with ONLY: ok" --model "Gemini 3.5 Flash (Medium)"`, 90 s cap, no wrapper) **also hung** — exit 124, 0 bytes stdout, empty stderr, and a single-line `--log-file` (just `Raising signal 15` at kill, no serving output, no auth/quota markers). So the hang is inside the `agy` CLI / Antigravity serving path during this session, not in `agy-agent`:

- The workspace **is trusted** (`trustedWorkspaces` contains the CWD — index 3), so it is not the trust-prompt hang.
- No `--add-dir` on a large tree and no concurrency (single serialized call), so neither known §10/§14 pathology applies.
- The log carries no `quota`/`auth`/`planner` diagnostic — agy emitted nothing before the SIGTERM, consistent with a backend serving stall (network/quota/auth on Antigravity's side), `agy` v1.0.6.

**Concern (recorded, not blocking):** agy's serving path was unreachable at validation time, so the three signal classes could NOT be positively confirmed in this session. Per the no-loop / no-block discipline this is recorded honestly and the migration proceeds — the wrapper's failure-detection (empty-stdout + log-tail → synthesized exit code) is the safety net and it fired correctly. **Re-run the three calls once agy serving recovers** to confirm grounded URLs, chrome absence, and bare-JSON parseability before fanning the migration out to the parallel consumers (x-research Task 12, x-qa Task 13).

### Replacement status
- `agy` is the SOLE Google-model backend. `gemini-agent` + `gemini_cli` are removed (Task 14).
- Re-pin model aliases if `agy models` changes the display strings (re-run `agy models`; update Task 3's `resolve_model` + the x-gemini table).
